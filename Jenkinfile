pipeline {
    agent any

    stages {
        stage('Clone') {
            steps {
                git 'https://github.com/logeshmathiyazhagan-create/Task-1.git'
            }
        }

        stage('Build') {
            steps {
                sh 'python3 --version'
            }
        }
    }
}
